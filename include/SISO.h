#ifndef __SISOH__
#define __SISOH__

/*
    get safely an int
    return 1 if stdin is not a num
    */
int read_int(int* nb);
    

void read_string(char* string, int size_str);
    /*
    get safely a string
    */

#endif